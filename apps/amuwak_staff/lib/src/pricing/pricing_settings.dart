// `PricingSettings` moved into `amuwak_core` (Customer App Phase C) so the
// customer app's estimate can reuse it. This re-export keeps the staff app's
// existing relative imports working unchanged.
export 'package:amuwak_core/models.dart' show PricingSettings;
